bin_PROGRAMS = fakeconnect
fakeconnect_SOURCES = fakeconnect.c socket.c capture.c

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
fakeconnect_LDFLAGS = $(all_libraries) 
noinst_HEADERS =  fakeconnect.h fakeconnect.h capture.h
